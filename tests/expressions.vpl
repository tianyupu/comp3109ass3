func bracket(a)
  a = (a);
end

func bracketedOperations(a, b)
  a = (a * a) + (b * b);
  a = (a + b) / 2.0;
  a = (a * a) + b + 10;
end

func mins(a,b)
  a = min(a, b);
  a = min(a + 1, b - 1);
  a = min(a, b) + 20;
end

func trickyOrderOperations(a, b)
  a = a + a * (b + b);
  a = a + b / 2.0;
  a = b + a * (b + 10);
end

func nested(a, b)
  var c;
  c = 1;
  a = (a * (b + 1));
  b = 2 * ((b - c * (1 + 1)) + 1);
end
